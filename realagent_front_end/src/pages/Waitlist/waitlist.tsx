
import { Flex } from '@mantine/core';
import Waitlist from './index'; // Import the index.js component

const WaitlistPage = () => {
  return (
    <Flex bg={"#FFFFFF"}>
  <Waitlist />;
  </Flex>
  )
};

export default WaitlistPage;
